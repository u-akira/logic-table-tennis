# ロジタク対戦ゲーム設計（XIAO ESP32C3 / config.h準拠）

## 1. ゲーム概要
- タイトル: ロジタクDUEL（仮）
- ジャンル: 対戦ボードゲーム（CPU mode / 2P mode）
- プレイ人数: 1人 or 2人
- 想定プレイ時間: 3〜8分/1ゲーム
- ひとことで説明: 限られた情報から最適手を選ぶ、短時間決着のロジック対戦

## 2. ルール

## 2.1 盤面ルール（確定）
- 卓球台を模したボードを使用する
- ボードは `6x6` タイルで構成する
- 中央で分割し、手前3段を「自陣」、奥3段を「敵陣」とする
- 段の呼び方:
  - 自陣は下から `後段` `中段` `前段` と呼ぶ
  - 敵陣は上から `後段` `中段` `前段` と呼ぶ
- 自陣/敵陣の境界線を画面上で明示する

## 2.2 カード構成（確定）
- カード総数は `15枚`
- 種別と数値内訳:
  - まっすぐ: `1, 2, 3, 4, 5`
  - 斜め右: `1, 2, 3, 4, 4`
  - 斜め左: `1, 2, 3, 4, 4`

## 2.3 ゲームの流れ（確定）
1. スタート時にタイトルを表示する
2. タイトルで `CPU mode` と `2P mode` を表示する
3. `LEFT` / `RIGHT` でモードを選択し、`UP` で決定する
4. `CPU mode` 決定時は、現在のゲーム内容（1人 vs CPU）を開始する
5. `2P mode` 決定時は、ESP-NOWを使用して `2P mode` が選択された端末を探索する
6. 相手端末を検出・接続できたら、2人対戦モードを開始する
7. 15枚のカードをシャッフルし、各プレイヤーに6枚ずつ配る
8. 相手の手札は非公開とする
9. サーブ順を決定する（初回ゲームはランダム）
10. サーバー手番:
   - 自陣の後段の任意マスを選択してボールを置く
   - 配置位置の決定後、出すカードを選択する（サーブは30秒制限の対象外）
   - 選択できるカードは自分の手札にあるカードのみ
11. 選択カードの内容どおりにボールを進める
12. 相手手番:
   - 直前の相手カードを表示する
   - 相手のボール位置確定後、30秒タイマーを開始する
   - 30秒以内にカードを選択する
13. 選択カードの内容どおりにボールを進める
14. 勝利条件が確定するまで 12〜13 を繰り返す
15. 勝利条件確定時、勝者を決定してスコアを表示する（1ゲーム取得）
16. 次ゲームを開始する（サーバー交代）
17. 先に3ゲーム取得したプレイヤーをマッチ勝者とする

## 2.4 勝利条件（確定）
- 以下のいずれかを満たした時点で、現在手番のプレイヤーを失点（相手がそのゲームの勝者）とする:
  - ボールが敵陣に届かない
  - ボールが盤面外にはみ出す
  - 30秒以内にカードを選択しない（サーブ時は除外）
  - 出せるカードが無くなる

## 2.5 2P表示ルール（確定）
- 通信上の盤面座標は共通の正規座標のまま扱う
- 各端末は自分視点で盤面を描画する
- 1P端末でも2P端末でも、自分側が常に手前に見えるようにする
- 表示は上下左右とも反転した完全な鏡像表示とする
- 相手の手札は表示しない
- 相手が出したカードは、相手側の捨て札として白黒反転で表示する
- カードの操作は自分の手番中だけ有効とする
- 相手手番中は、表示は自分視点のままでもカードの決定はできない
- ボールの動きは各端末の視点で矛盾しないように描画する

## 3. 操作設計（4ボタン前提）
- サーブ時の操作:
  - 自陣の後段にある3マスから配置位置を選択する
  - `LEFT` / `RIGHT` で選択位置を移動する
  - `UP` で配置位置を決定する
  - 配置位置決定後、手札から出すカードを選択して決定する
- サーブ時以外の操作:
  - 手札から出すカードを選択して決定する
- 相手のカード選択中の操作:
  - カード選択カーソルの移動は可能
  - 決定入力は無効（カード確定はできない）

## 4. 画面設計（OLED 128x64想定）
- 解像度/座標系: 128x64, 左上(0,0)
- HUD要素: 手番、ターン数、プレイヤー/CPUスコア、CPUレベル
- プレイ画面レイアウト:
  - 左側: `6x6`盤面を表示（セルは正方形ではなく縦長の長方形）
  - 横中央ライン（自陣/敵陣の境界線）は太線で表示
  - 縦中央ラインは横中央ラインより細く、通常グリッド線より太く表示
  - 右上: カード選択カウントダウンタイマー（30秒）
  - 右中央: 直前に選択されたカードを表示（自分/相手の双方を表示）
  - 右下: 自分の手札を表示（最大6枚、可能なら1列表示）
- 2P時の表示ルール:
  - 盤面は各端末で完全鏡像表示にする
  - 画面上では自分側が常に手前になるようにする
  - 相手の手札は表示しない
  - 相手の捨て札は白黒反転して表示する
  - ボールの進行方向は自分視点で自然に見えるようにする
- タイトル画面:
  - `Logic Table Tennis` を表示する
  - `CPU mode` と `2P mode` を表示する
  - `LEFT` / `RIGHT` でモード選択、`UP` で決定する
  - `press any button` は表示しない
  - `CPU mode` を押した時は今のゲーム（1人 vs CPU）を開始する
  - `2P mode` を押した時はESP-NOWで相手端末の探索を開始する
- リザルト画面: 勝者、最終スコア、再戦選択
- フォント/可読性ルール: 8pxグリッド基準、重要情報は反転表示

## 4.1 タイマー表示ルール（確定）
- 制限時間は30秒
- タイマー開始条件: 相手のボール位置が確定した直後
- サーブ時は30秒制限の対象外
- 0秒到達時は時間切れとして勝利条件判定を行う

## 4.2 カードデザイン（確定）
- カード上部に方向を示す矢印を表示する
  - 上: `↑`
  - 斜め右: `↗`
  - 斜め左: `↖`
- カード下部に数値を表示する
  - 数値は「ボールが進むマス数」を表す

## 5. サウンド設計（BUZZER）
- 効果音一覧:
  - クリック音: サーブ位置選択時、カード選択時に再生
  - 決定音: 決定時に再生
  - 勝利条件確定音: 勝利条件が確定した時に再生
  - ミス音: 短い下降1音
- 音長・音程のルール: 30〜180ms、連続発音時は10ms無音を挟む
- 同時発音の優先度: 勝敗演出 > 決定音 > カーソル移動音

## 6. NeoPixel演出（3 LEDs）
- LED0の役割: プレイヤー状態表示
- LED1の役割: ニュートラル/手番インジケータ
- LED2の役割: CPU状態表示
- 状態別カラー定義:
  - 勝利: LED3つを青色で点灯
  - 敗北: LED3つを赤色で点灯
  - 通常: 取得ゲーム数の数だけ緑色で点灯（最大3つ）
- 点滅/フェード仕様: 100ms周期フラッシュ、通常時は固定点灯
